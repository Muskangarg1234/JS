//  Q1-> generate a random number between 1 and 100

let random=Math.floor(Math.random()*98)+2;
console.log(random);

// if we want to generate from 1 to 100 (i.e 1 and 100 included)
random=Math.floor(Math.random()*100)+1;
console.log(random);

// Q2-> generate a random number between 1 and 5
random=Math.floor(Math.random()*3)+2;
console.log(random);

// if we want to generate from 1 to 5 (i.e 1 and 5 included)
random=Math.floor(Math.random()*5)+1;
console.log(random);

// Q3-> generate random integer from 21 to 25(21 and 25 included)
random=Math.floor(Math.random()*5)+21;
console.log(random);